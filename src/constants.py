"""Lightweight shared constants with no heavy imports, so the UI can use them
without pulling in TensorFlow. Single source of truth for the class scheme."""

IMG_SIZE = 224
CLASS_NAMES = ["No_DR", "Mild_Moderate", "Severe_Proliferate_DR"]
